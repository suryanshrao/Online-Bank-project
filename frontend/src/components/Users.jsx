import { useEffect, useState } from "react"

export const Users = () => {

    // Replacing with backend call
    const [users, setUsers] = useState([]);
    const [filter, setFilter] = useState("");

    useEffect(() => {

    })
}